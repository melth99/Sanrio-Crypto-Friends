<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>

<body>
    <h1>Mymelody presents</h1>
    <p>
        <%=foundPort.portName%>
    </p>
    <ul>
        <% if (foundPort.coins) { %>
            <% foundPort.coins.forEach(coin=> { %>
                <li>
                    <%=coin.coinName %>
                </li>
                <%});%>
                    <% } %>
    </ul>
</body>

</html>